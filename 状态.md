# 项目状态

> 更新时间：2026-09-07 · 当前版本 v0.9.6 · 单元测试 141 用例全绿（`python -m unittest discover tests`）
> 配套文档：README.md（使用说明）· 记录.md（问题记录）· 跨平台规划.md（跨平台方案）
> 代码仓库：<https://github.com/NotUNperson/CampusNet_AutoLogin> · 旧版单文件程序归档：<https://github.com/NotUNperson/SchoolNework_auto_login_selfuse>

## 一、当前可用功能

- 校园网自动登录（隐藏浏览器注入 + 确认弹窗自动点击 + 网络级兜底判定）
- 一键退出登录（含 manual 强制手动模式、browser/window 兜底）
- 系统托盘：状态变色（绿/蓝/黄/红/灰）+ 呼吸动画 + 迷你看板 + 状态菜单
- 临时计时器：按时长 / 到点退出 / 自定义延长 / 命令行 `--timer` `--until` `--once`
- 深色模式主题（跟随系统 / 浅色 / 深色）
- 新手引导、环境隔离、滚动文件日志、系统气泡通知、单次运行模式

## 二、清点发现的问题（2026-08-31 目录清点）

1. [ ] **隔离机制误触发**：项目根目录无 config.ini 时，文档/构建产物被杂乱检测判为"杂项"（阈值 2），每次在根目录运行 `python run.py` 都触发迁移 → 已积累 **7 个隔离文件夹**。
   修复方向二选一：源码模式跳过杂乱检测（该检测本为 U 盘 exe 场景设计）/ 更新白名单（*.md、*.spec、.zcode、旧版存档等）
2. [ ] **dist 里的 exe 落后 4 个版本**：`dist/CampusNet_AutoLogin.exe` 为 v0.8.2 时期构建（08-30 18:51），v0.8.3~v0.9.0 的修复与功能（假断开修复、manual 模式、图标颜色修复、稳定性三连修、深色模式）均未包含 → `pip install pyinstaller` 后重跑 build.bat
3. [ ] **build-2.bat 与 build.bat 重复**：同样的 pyinstaller 命令，仅编码不同（ANSI、无 chcp），建议二留一

## 三、优化建议清单

### A 健壮性
- [ ] 单实例互斥（Windows 命名互斥量）：目前可多开，会出现双托盘、双守护同时登录、config 写冲突
- [ ] 休眠唤醒立即检测：识别时钟跳变（本次检测距上次远超周期）后立即跑一轮，不用等 45 秒
- [ ] HTTP 204 探测 + srun HTTP 直连登录（跨平台规划 Phase 1）：登录亚秒级、探测语义更准、三端铺路

### B 功能
- [ ] 开机自启开关（写启动文件夹）
- [ ] exe 图标（PyInstaller `--icon`，目前是默认图标）
- [ ] 看板日志页签
- [ ] 托盘 tooltip 显示状态/剩余时间
- [ ] 失败指数退避（当前固定 retry_delay）

### C 代码质量
- [ ] 类型标注 + ruff/mypy 静态检查
- [ ] 计时器对系统时钟跳变的鲁棒性（休眠/手动改时钟场景）

### D 有意搁置（尊重既定决定，勿反复提议）
- 密码 DPAPI 加密（两次确认为明文）
- 深浅色跟随的系统弹窗样式（影响极小）

## 四、目录清理清单（按约定不自动删除，需手动执行）

- [ ] 6 个旧隔离文件夹：`CampusNet_Isolated_Env`、`*_141322`、`*_143201`、`*_235001`、`*_003925`、`*_010042`（合计 ~1.9MB，均为旧版本副本；`*_012342` 为最新，确认不用后再删）
- [ ] `build/`（48MB，PyInstaller 中间产物，重新打包会再生）
- [ ] `__pycache__`
- **保留**：`dist/`（当前运行环境：exe + config.ini + 日志）、`CampusNet_AutoLogin.spec`（打包配置）、`CampusNet_AutoLogin_v004_0408.py`（旧版存档，已归档至独立仓库 <https://github.com/NotUNperson/SchoolNework_auto_login_selfuse>，含 v003/v004 标签）

> ⚠️ 敏感信息：`dist/config.ini` 及各隔离文件夹内的 config.ini 均**含明文密码**，整个目录不要直接压缩分享或上传代码托管平台。

## 五、跨平台（详见 跨平台规划.md）

- [ ] Phase 1（地基，Windows 先行）：HTTP 204 探测 + srun HTTP 直连登录 + LoginBackend 抽象
- [ ] Phase 2：Android——Termux 脚本（detect/login/daemon）+ MacroDroid 触发指南
- [ ] Phase 3：Linux——PlatformAdapter + systemd 用户服务 + `--no-tray`
- [ ] Phase 4：macOS——launchd LaunchAgent + osascript 通知

## 六、质量基线

- 单元测试：76 用例（config / login JS / status / cli / tray / daemon / ui），`python -m unittest discover tests`
- 冒烟：`--once` 模式自行退出（退出码 0）为常规回归信号
- 已知限制与排障：见 README「日志与排障」；历史问题与根因：见 记录.md
