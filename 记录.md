# 开发问题记录

记录校园网自动登录守护工具从 v0.5 重构到 v0.9 过程中遇到的实际问题，按主题归类。每条含「现象 → 根因 → 解决 → 要点」，供日后维护与同类项目参考。

## 版本时间线

| 版本 | 日期 | 内容 |
|---|---|---|
| v0.5.0 | 2026-08-30 | 单文件重构为模块化包，修复首批 bug |
| v0.6.0 | 2026-08-30 | 设置窗口 + 托盘菜单 + 气泡通知 |
| v0.7.0 | 2026-08-30 | 迷你看板 + 计时器/命令行 + 新手引导 |
| v0.8.0–0.8.2 | 2026-08-30 | 一键退出登录、托盘变色、退出登录兜底 |
| v0.8.3 | 2026-08-30 | 网络判定防误报 + manual 手动退出模式 |
| v0.8.4 | 2026-08-30 | 修复托盘图标颜色冻结 |
| v0.8.5 | 2026-08-31 | 托盘图标稳定性三连修 |
| v0.9.0 | 2026-08-31 | 深色模式主题 |

---

## 一、打包脚本与终端

### 1. build.bat 中文乱码

- **现象**：双击运行 build.bat，中文注释和提示全部乱码，甚至整行被解析断裂成无效命令
- **根因**：cmd 按"控制台当前代码页"逐字节解码批处理，没有任何编码探测机制。中文 Windows 默认 GBK(936)，而文件是 UTF-8 保存——编码器/解码器错配。UTF-8 的 3 字节汉字被 GBK 按 2 字节重新配对，产生"娴嬭瘯"类乱码；更糟的是 GBK 第二字节可落入 ASCII 区间，把后面的空格/字母吞掉，破坏行结构
- **解决**：文件开头加 `chcp 65001 >nul`（把解码器切到 UTF-8 匹配文件），文件保存为 UTF-8 无 BOM + CRLF 换行；`chcp` 之前的行必须纯 ASCII
- **要点**：bat 没有 Python 那样的编码声明机制；"存成 ANSI(936)"是另一个等效方案（适配中文 Windows 但锁死 locale），**两者不能混用**——GBK 文件配 `chcp 65001` 会再乱一次

### 2. Ctrl+C 无法终止进程 / 关闭终端窗口后托盘残留

- **现象**：终端里按 Ctrl+C 程序毫无反应；直接关闭终端窗口则留下幽灵托盘图标
- **根因**：主线程阻塞在 webview 的原生 GUI 循环（pythonnet/WinForms 消息循环）里，Python 的 signal 处理必须等主线程回到字节码循环才执行——永远等不到。关闭终端的 CTRL_CLOSE_EVENT 同理，约 5 秒宽限后系统强杀，任何 Python 清理代码都不会运行
- **解决**：用 ctypes 注册 Win32 `SetConsoleCtrlHandler`——Windows 在独立线程调用它，完全不依赖 Python 主线程状态，可同时拦截 Ctrl+C、关终端、注销、关机。处理器中：记日志 → 移除托盘图标 → `os._exit(0)`
- **要点**：GUI 程序的信号处理要在 Win32 层拦截；回调函数指针必须保存模块级引用防止被 GC 回收成野指针

---

## 二、退出路径（本项目教训最集中的一块）

### 3. 退出时崩溃弹窗 + 进程卡死

- **现象**：退出时弹出 WinForms 错误对话框 `PythonException: 'NoneType' object has no attribute 'BrowserProcessId'`，进程卡死只能强杀
- **根因**：从守护线程调用 pywebview 的 `window.destroy()`。WebView2 清理路径在 WinForms 的 FormClosed 事件里访问了已置空的对象，pythonnet 把这个 Python 异常抛进 .NET 事件分发——而 pystray/webview 主循环都已阻塞，对话框和进程一起僵死
- **解决**：**所有退出路径彻底不销毁窗口**。统一模式：拦截关闭事件（返回 False 否决）+ 定时 `os._exit(0)`，资源随主进程由系统回收。程序里现在找不到任何一处 `window.destroy()`
- **要点**：跨线程操作 GUI 库的销毁/关闭接口是高危动作；"让系统随进程回收"往往比"优雅清理"更可靠

### 4. 幽灵托盘图标

- **现象**：程序退出后托盘图标还在，鼠标划过任务栏才消失
- **根因**：`os._exit` 直接终止进程，没有向系统发送移除图标的消息（Shell_NotifyIcon 的 NIM_DELETE）
- **解决**：所有退出路径先执行 `icon.visible = False`（pystray 会同步执行 NIM_DELETE，且可在任意线程调用），再结束进程。Ctrl+C、关终端、托盘退出、计时到期、单次完成、配置未完成关窗六条路径全覆盖
- **限制**：`taskkill /F` 强杀不给任何清理机会，任何程序都无法避免；已经残留的幽灵属于死进程，只能鼠标划过清除

### 5. 统一退出出口

- 退出路径一度散落在托盘退出、shutdown 回调、设置窗关闭等多处，各自维护清理逻辑
- 收敛为 `_shutdown(reason)` 单一出口：日志 → 气泡 → 移除托盘 → os._exit；新退出场景一律接它

---

## 三、托盘图标（状态变色 + 呼吸动画）

### 6. 图标颜色完全冻结（最隐蔽的一个）

- **现象**：图标永远停在初始绿色，变色/呼吸全部失效，日志毫无痕迹
- **根因**（三因叠加，经逐行核对 pystray 0.19.5 源码确认）：
  1. 动画线程在 `icon.run()` 之前启动，首帧赋值发生在图标置为可见之前——pystray 对不可见图标的赋值"只存图、不渲染、不报错"
  2. 动画器把"赋值没抛异常"当成"渲染成功"，把这个从未显示的帧记入去重账本 `_last_drawn`
  3. 守护每 45 秒推的都是同色，去重看到"与账本一致"就跳过——丢失的帧**永不重试**，图标冻结
- **解决**：动画线程改到 pystray 的 setup 回调中启动（回调里先 `visible = True`）；赋值失败清空账本下一轮重试；每 3 秒无视账本强制重绘一次兜底；失败日志升为 warning
- **要点**：**"赋值不报错 ≠ 生效"**。对托盘、GUI 这类外部 UI API，光有刷新代码不够，必须有"失败可发现 → 可重试"的闭环，外加低频强制兜底刷新

### 7. 登录成功后卡在呼吸态

- **现象**：自动登录成功后，图标呼吸最长挂 45 秒才变色
- **根因**：登录成功分支只调了状态更新，**忘了推送新图标颜色**——呼吸态一直持续到下一轮检测被覆盖
- **解决**：登录成功瞬间推送连接态颜色（蓝/绿）。这是唯一一个真正"没写刷新代码"的地方

### 8. 计时器的蓝色始终不出现

- **现象**：启动计时器后托盘不变蓝
- **根因**：计时器操作只改了内部状态，**不唤醒沉睡中的守护循环**（检测周期 45 秒）。1 分钟的计时器经常在沉睡中过期并直接走退出流程，蓝色窗口为 0
- **解决**：计时器的启动/到点/取消均唤醒守护循环立即重评一轮，蓝色即时生效并覆盖全生命周期

### 9. 颜色时好时坏（偶发丢失）

- **现象**：大部分时候正常，偶发某次变色丢失
- **根因**：pystray 内部完全没有锁。气泡通知（守护线程调 `icon.notify`，本质也是一次图标区写操作）与动画线程的图标重绘并发操作同一个 NOTIFYICONDATA，竞态时更新静默丢失
- **解决**：气泡通知入队，由动画线程统一串行发送——**所有对 pystray 图标区的操作收敛到一个线程**；blocked/offline 状态每轮无条件重推，配合 3 秒强制重绘，丢失必定自愈
- **要点**：操作无锁的第三方对象时，把所有写操作串行化到一个线程

---

## 四、一键退出登录（与门户的斗争）

### 10. "已点击注销按钮，但未确认到退出成功"

- **现象**：日志显示按钮点到了，但网络没断，退出失败
- **根因**：门户（srun theme=pro）点击注销后弹出确认框，其样式不在常见弹窗库（SweetAlert/Layer/Bootbox）的选择器覆盖范围内，隐藏窗口里无人点"确定"，注销请求根本没发出
- **解决**（层层递进）：① 通用确认弹窗自动点击；② 成功判定放宽为"注销按钮从页面消失"；③ **网络级兜底**——DOM 未确认时 ping 外网，会话真被注销则网络必然断开（唯一客观事实）；④ 失败时输出页面诊断日志（URL + 关键元素存在性）；⑤ 新增 `manual` 模式——完全跳过自动点击，直接弹可见窗口人工操作，适配任何非标准门户
- **要点**：对外部网页 UI 的自动化必须留人工出口和诊断快照，永远不要假设门户结构

### 11. 网络明明通，却被提示"外网已断开"

- **现象**：browser 兜底模式下误报退出成功
- **根因**：判定"断开"用的是**单次 ping**（2 秒超时，一次定生死）。单次 ICMP 存在偶发丢包/超时，恰巧失败即误判
- **解决**：`is_network_down_confirmed`——连续 3 次 ping（间隔 1.5 秒）全部失败才判定断开，任一次成功即判仍在网（误报概率 p → p³）。两处调用点（退出结果判定、手动退出结果播报）统一替换
- **要点**：网络状态的否定结论（"不通"）必须多次确认，肯定结论（"通"）单次即可

---

## 五、逻辑与时序类

### 12. 守护循环条件写反

- **现象**：冒烟测试发现配置有效时打印"配置未完成"
- **根因**：`validate()` 返回问题列表，`if validate(config):` 的真值语义弄反（非空列表=有问题）
- **解决**：改为 `problems = validate(config); if not problems: ...`，并把问题列表打进日志
- **要点**：对"返回列表/集合"的判空，宁可先赋值再判断，不要内联在 if 里凭感觉写

### 13. 暂停状态下计时器到期不退出

- **根因**：暂停等待循环里没有轮询计时器
- **解决**：暂停循环内同样检查到期，走统一退出。计时器是墙钟时限，暂停不能豁免

### 14. --timer 在等待配置阶段就开始倒计时

- **现象**：首次运行若花很久填配置，守护刚启动就"计时结束"退出
- **解决**：计时从守护真正开始时起算（coordinator 配置校验通过后启动），等待配置的时间不计入

### 15. 守护循环异常后停在呼吸态

- **根因**：异常分支只记日志，状态机停在 LOGGING_IN 的呼吸色
- **解决**：异常处理中推回基色（非呼吸），等待下一轮重新判定

---

## 六、测试与配置

### 16. 测试断言与实现语义不符（两次）

- 强制重绘计数器是全局连续的（每第 10 tick），测试按"首帧后再数 10 个"写错
- 呼吸不改变基色（呼吸只是基色脉动），异常恢复测试期望写成呼吸目标色
- **要点**：写断言前先写清楚实现的语义约定；失败往往不是实现错，而是对语义的理解分歧——要停下来分辨是哪种

### 17. 新增配置字段的全链路缺漏

- **现象**：v0.9.0 加 `ui_theme` 字段后 10 个测试报 AppConfig 缺参数
- **根因**：新增字段要同步的位置比想象多：默认值表 / 读取与钳制 / 保存 / 表单解析（parse_form 构造校验对象）/ 测试夹具。且 parse_form 返回的字段集合决定了"表单保存会覆盖什么"——主题不在表单里，parsed 就绝不能含它，否则保存表单会静默覆盖用户设置的主题
- **解决**：parse_form 候选对象以占位值补齐（validate 不检查该字段）；返回集不含它；另做"表单保存不覆盖主题"的回归验证
- **要点**：数据类加字段 = 一条全链路检查单；"哪些字段会被哪个写入路径覆盖"要显式想清楚

---

## 七、调试环境经验（Windows + Git Bash）

- 子进程里 `chcp` 会污染共享控制台的代码页，导致之后 bash 自己的中文输出也乱码——验证脚本应输出到文件再读，或全程 ASCII
- Git Bash 的 `kill $!` 杀掉的是包装进程，Windows 的 python.exe 子进程会残留——清理时先查命令行确认归属，再按 PID `taskkill //F //PID`
- 后台化 `A && B && python ... &` 会把整条 && 链放进子壳，变量赋值丢失——分段写语句
- `python -c` 里访问 `/tmp/...` 路径不通（那是 MSYS 虚拟路径），要转 Windows 路径或用管道喂内容
- `--once`/定时等自带退出的冒烟用 `timeout N` 包裹，exit code 124 即"未按预期自行退出"，是发现退出 bug 的关键信号

---

## 八、通用原则提炼

1. **退出要单一出口**：GUI 程序所有退出路径收敛到一个函数，清理动作（移除托盘、落日志）集中做
2. **赋值不报错 ≠ 生效**：外部 UI API 的更新要有失败发现与重试闭环 + 低频强制兜底
3. **否定结论需多次确认**：网络"不通"这类判定必须连续多次失败才可信
4. **无锁第三方对象 → 写操作串行化**：所有 mutation 收敛到一个线程
5. **自动化必须留人工出口**：对外部网页/UI 的自动化不可能覆盖所有结构，兜底是"打开给人操作" + 诊断快照
6. **数据类加字段走检查单**：默认表 → 读取（含钳制）→ 保存 → 表单解析 → 测试夹具，缺一不可
7. **bat/控制台的编码问题**：先确认"文件编码"与"解码代码页"是否一致，再谈其他
