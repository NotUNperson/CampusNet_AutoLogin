# 跨平台规划（Android / Linux / macOS）

> 状态：**规划稿，未实施**。本文档回答"这个工具要走到别的平台，需要考虑什么、按什么顺序做"。
> 现有 Windows 版本为 v0.9.0，架构与问题记录见 README.md 与 记录.md。

---

## 0. 现状盘点：什么能带走，什么带不走

| 类别 | 内容 | 结论 |
|---|---|---|
| **可移植核心** | 守护状态机（daemon 状态机逻辑）、config、TimerState、AppStatus、login 的 JS 生成思路、ui_html 页面 | 逻辑可直接复用 |
| **绑死 Windows** | `winapi.py`（MessageBoxW / SetConsoleCtrlHandler）、`os.startfile`、EdgeChromium 后端（pythonnet/WinForms）、PyInstaller `-w`、ms-settings 跳转 | 每平台需重写对应适配层 |
| **伪跨平台（有坑）** | pystray（win32 / Linux-AppIndicator / macOS-rumps 三后端能力各异）、ping3（**Windows 免 root，Linux/Android 需 root 或 CAP_NET_RAW**）、pywebview（三端 GUI 依赖完全不同，Android 直接不可用） | 不能想当然搬过去 |

**关键架构结论**：浏览器注入登录在 Android 上不可行（无显示服务器），在 Linux/macOS 依赖沉重且各有线程约束。**跨平台的钥匙是 srun HTTP 直连登录**——纯 requests 实现，无浏览器依赖，秒级登录。我们的门户已确认是 srun（日志：`srun_portal_success`、`ac_id=1`、门户 IP 192.168.57.33）。

---

## 1. 通用改造（三端受益的前置，先在 Windows 上做）

### 1.1 登录后端抽象 `LoginBackend`
```
LoginBackend
├── InjectBackend   # 现有 pywebview 注入（Windows 主力，其他平台可选降级）
└── SrunHttpBackend # srun HTTP 直连（跨平台主力，Android 唯一选择）
```

### 1.2 srun HTTP 直连协议要点
- 流程：`GET /cgi-bin/get_challenge?username=...&ip=...` 拿 challenge → 按 srun 算法编码 `info` 与密码 → `POST /cgi-bin/srun_portal`（action=login）→ 解析 JSON `res=="ok"`；登出为 `action=logout`
- 参数需实测确认：`ac_id`（日志显示 ac_id=1）、校验算法变体（md5 vs hmac-md5）、`enc` 参数——**在校网环境抓一次真实登录请求即可锁定**
- 开源参考：GitHub 上 srun 登录脚本众多（协议公开），实现成本低
- 优点：无浏览器依赖、亚秒级登录、任何平台可跑；局限：仅适配 srun 门户（非 srun 门户仍走注入后端）

### 1.3 HTTP 204 探测后端（替代 ping 的跨平台探测）
- 访问 `generate_204` 类地址（如 `http://connect.rom.miui.com/generate_204` / gstatic）：**204 = 在线；302/重定向到门户 = 被拦截**
- 为什么必须：ping3 在 Linux/Android 需要 root 或 CAP_NET_RAW（Termux 无 root）；且 HTTP 语义直接看见"被门户劫持"，比 ICMP 更准
- ping 保留为 Windows 快捷路径；探测后端同样抽象化

### 1.4 平台适配层 `PlatformAdapter`
| 能力 | Windows（现有） | Linux | macOS | Android/Termux |
|---|---|---|---|---|
| 配置路径 | `%LOCALAPPDATA%` 或程序目录 | `~/.config/campusnet/` | `~/Library/Application Support/campusnet/` | Termux home |
| 通知 | pystray 气泡 | notify-send / libnotify | `osascript display notification` | `termux-notification` |
| 退出信号 | SetConsoleCtrlHandler（已有） | SIGINT/SIGTERM 标准处理 | 无 GUI 时同 Linux | 进程由 MacroDroid 生命周期管理 |
| 托盘 | pystray win32（已有） | pystray AppIndicator（可选） | rumps（可选） | 无（通知即 UI） |
| 自启动 | 计划任务/启动文件夹 | systemd --user unit | launchd LaunchAgent | MacroDroid 触发 |
| 环境隔离 | 保留（U 盘场景） | 禁用（XDG 路径无重名问题） | 禁用 | 不需要 |

---

## 2. Android = Termux 脚本 + MacroDroid 调度（优先级最高）

**形态**：不移植完整应用。Termux 只跑独立脚本（登录 + 检测），MacroDroid 当"系统调度器 + UI"。

### 2.1 依赖与安装
```bash
pkg install python
pip install requests        # 仅此一个依赖
```
不需要 pywebview / ping3 / pystray / PIL。

### 2.2 脚本 `campusnet.py` 三种模式
- `detect`：HTTP 204 探测，输出 在线/被拦截/离线
- `login`：srun 直连登录一次，成功/失败经 `termux-notification` 反馈
- `daemon`：`termux-wake-lock` + 轮询循环（检测失败→登录→指数退避），复用守护状态机思路
- 配置：`~/.config/campusnet/config.json`（URL/账号/ac_id/探测地址）

### 2.3 MacroDroid 集成（触发器）
| 触发 | 动作 | 说明 |
|---|---|---|
| Wi-Fi 连接 / SSID 变化 | 触发 `login` | 主路径 |
| 定时（如每 5 分钟） | 触发 `daemon` 检查 | 兜底重试（Android 杀后台是常态） |
| 设备开机 | 启动 `daemon` | 需 Termux:Boot 或 MacroDroid 开机事件 |

集成两条路径：
1. **免费**：`am broadcast -a termux.RUN_COMMAND`（需在 Termux 的 `~/.termux/termux.properties` 中开启 `allow-external-apps=true`，且 MacroDroid 与 Termux 签名一致）
2. **付费**：Termux:Tasker 插件（MacroDroid 原生支持调用，更省事）

### 2.4 Android 特有风险与对策
- **厂商杀后台 / Doze**：`termux-wake-lock` + 关闭 Termux 电池优化（加入白名单）+ MacroDroid 周期触发兜底——三层保险缺一不可
- **无 root 时禁止 ICMP**：已由 HTTP 204 探测规避（这是不用 ping3 的硬理由）
- **无托盘/无窗口**：MacroDroid 的通知与状态栏就是 UI
- **iOS 无 Termux**：不在规划范围

---

## 3. Linux

两种形态，按需选用：

### 3.1 A. 无头 systemd 用户服务（主推）
- 适配场景：宿舍常开设备、服务器、路由器旁的小主机；也适合不需要托盘的桌面
- 登录后端：srun HTTP 直连（无需任何 GUI 依赖）；探测：HTTP 204
- 服务文件 `~/.config/systemd/user/campusnet.service`（草案）：
  ```ini
  [Unit]
  Description=CampusNet Auto Login Daemon
  After=network-online.target

  [Service]
  ExecStart=%h/.local/bin/campusnet daemon
  Restart=on-failure
  RestartSec=10

  [Install]
  WantedBy=default.target
  ```
- 信号处理：Linux 允许"主线程=Python 监督循环、GUI 放子线程"，SIGINT/SIGTERM 标准处理即可（无 Windows 的 GUI 循环阻塞问题）
- 日志：`~/.local/state/campusnet/`；通知：notify-send（桌面会话内有效，纯服务模式只落日志）

### 3.2 B. 桌面托盘（可选）
- pystray 走 AppIndicator 后端，需系统包 `gir1.2-ayatanaappindicator3-0.1`
- **GNOME 默认不显示托盘图标**（需装 AppIndicator 扩展）；KDE / XFCE / MATE 开箱即用
- 因此托盘做成 `--no-tray` 可选项，服务形态默认无托盘

### 3.3 其他
- pywebview 注入登录在 Linux 可用（GTK + WebKit2 后端），但需 apt 安装 `libwebkit2gtk` 一串依赖 → 定位为"非 srun 门户才装"的降级项
- 环境隔离（isolate）在 Linux **禁用**：配置走 XDG 路径，不存在"config.ini 重名"焦虑
- 打包：源码 + venv 为主；PyInstaller 可选

---

## 4. macOS

**最大约束**：pywebview/Cocoa 必须在主线程运行 → 与"主线程监督循环 + 信号处理"冲突；且 Gatekeeper 会拦截未签名的 PyInstaller 产物。

### 4.1 推荐形态：无头 LaunchAgent + 纯 HTTP 后端
- 无 GUI → 主线程自由 → SIGINT/SIGTERM 标准处理
- 自启动：`~/Library/LaunchAgents/com.campusnet.daemon.plist`（草案）：
  ```xml
  <plist version="1.0">
  <dict>
    <key>RunAtLoad</key><true/>
    <key>KeepAlive</key><true/>
    <key>ProgramArguments</key>
    <array><string>/usr/bin/python3</string><string>…/campusnet.py</string><string>daemon</string></array>
    <key>StandardOutPath</key><string>…/campusnet.log</string>
  </dict>
  </plist>
  ```
- 通知：`osascript -e 'display notification "…" with title "校园网守护"'`
- 日志：`~/Library/Logs/campusnet/`；配置：`~/Library/Application Support/campusnet/`

### 4.2 菜单栏（可选，后期）
rumps（macOS 原生菜单栏框架）可替代 pystray——它自带 NSApplication 主线程，可承载定时器与状态刷新。作为后期增强，不在首期。

### 4.3 注入登录的定位
若坚持注入登录：主线程跑 pywebview + 接受终端 Ctrl+C 失效（与 Windows 同病，可用 PyObjC 桥解决但复杂度不值）→ 不建议。macOS 上 srun 直连后端即可覆盖。

### 4.4 分发
未签名的 PyInstaller 产物会被 Gatekeeper 拦截（需右键绕过或付费公证）→ **推荐源码 + venv 安装脚本**。

---

## 5. 平台能力对照总表

| 能力 | Windows（现状） | Android/Termux | Linux | macOS |
|---|---|---|---|---|
| 探测 | ping + HTTP204（建议补） | HTTP204 | HTTP204（ping 需特权） | HTTP204 |
| 登录 | 注入 + srun 直连（建议补） | **srun 直连（唯一）** | srun 直连主推 / 注入可选 | srun 直连主推 |
| UI | 设置窗 + 看板 + 托盘 | MacroDroid 通知 | systemd / 托盘（可选） | LaunchAgent / 菜单栏（可选） |
| 自启动 | 启动文件夹 | MacroDroid 触发 | systemd --user | launchd LaunchAgent |
| 通知 | pystray 气泡 | termux-notification | notify-send | osascript |
| 打包 | PyInstaller -F -w | 源码直跑 | 源码 + venv / PyInstaller | 源码 + venv（Gatekeeper） |
| 隔离机制 | 保留 | 不需要 | 禁用 | 禁用 |

---

## 6. 落地顺序建议

1. **Phase 1（地基，先在 Windows 做）**：HTTP 204 探测 + srun HTTP 直连后端 + LoginBackend 抽象，校园网实测登录成功 → Windows 版获得"秒级登录"能力，同时为三端铺路
2. **Phase 2（Android）**：独立 Termux 脚本（不依赖主代码仓库）+ MacroDroid 配置指南
3. **Phase 3（Linux）**：平台适配层（paths/notify/signal）+ systemd unit + 安装脚本
4. **Phase 4（macOS）**：launchd + osascript 通知适配
5. 各平台的托盘/GUI 永远是可选项，无头形态优先

---

## 7. 风险与待验证

| 风险 | 影响 | 对策 |
|---|---|---|
| srun 校验算法变体（md5 vs hmac-md5、enc、ac_id）未确认 | 直连登录写不对 | 校园网抓一次真实登录请求即可锁定（条件已具备） |
| Android 厂商激进杀后台 | daemon 被杀 | MacroDroid 周期触发 + 电池白名单 + wake-lock 三层兜底 |
| GNOME 不显示托盘 | Linux 桌面用户看不到图标 | 托盘做成可选项；服务形态为主 |
| 未签名二进制被 Gatekeeper 拦 | macOS 分发受阻 | 源码 + venv 分发 |
| 非 srun 门户的移植需求 | HTTP 后端不适用 | 保留注入后端，按平台评估可行性 |
| iOS | 无 Termux，无法运行 | 不在规划范围 |

---

## 8. 立项时的任务清单草案（复制即用）

- [ ] Phase 1：`LoginBackend` 抽象 + `SrunHttpBackend`（get_challenge/srun_portal/编码算法）+ HTTP204 探测 + 校园网实测
- [ ] Phase 2：Termux 脚本 `campusnet.py`（detect/login/daemon）+ config.json + termux-notification + MacroDroid 指南（含 allow-external-apps 配置）
- [ ] Phase 3：PlatformAdapter（paths/notify/signal）+ systemd unit 模板 + `--no-tray` + 安装脚本
- [ ] Phase 4：launchd plist 模板 + osascript 通知 + 安装脚本
- [ ] 每阶段收尾：单元测试（探测/登录编码可离线测）+ 对应平台文档章节
